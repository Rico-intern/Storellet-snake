{
  "name": "storellet-snake-supabase",
  "version": "2.0.0",
  "private": true,
  "description": "Standalone Storellet Snake Game with Supabase persistence.",
  "scripts": {
    "check:json": "node -e \"JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package.json OK')\""
  },
  "engines": {
    "node": ">=18"
  }
}
